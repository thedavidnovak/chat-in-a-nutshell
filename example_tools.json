[
    {
        "name": "reverse_string",
        "description": {
            "type": "function",
            "function": {
                "name": "reverse_string",
                "description": "A tool function used to reverse a given string.",
                "strict": true,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "input_string": {
                            "type": "string",
                            "description": "The string to be reversed"
                        }
                    },
                    "required": ["input_string"],
                    "additionalProperties": false
                }
            }
        },
        "definition": "def reverse_string(input_string):\n    return input_string[::-1]"
    }
]